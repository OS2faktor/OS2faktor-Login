CREATE TABLE persons_attributes_set (
  id                           BIGINT NOT NULL PRIMARY KEY AUTO_INCREMENT,
  name                         VARCHAR(255) NOT NULL,
  display_name                 VARCHAR(255) NULL
);
